condition ? exprIfTrue : exprIfFalse 를

exprIfTrue ?? exprIfFalse 로 축약가능하다

예를들어서

const x = a ?? 0 

이문법이 a 가 있으면 a를 쓰고 아니면 0 을 쓰겠다는말임

예를들어서

<ProfileImage image={user?.avatar ?? ''} />

이부분
user?.avatar 가 있으면 image={user?.avatar} 하고 아니면 

image={" "} 하겠다는말 
