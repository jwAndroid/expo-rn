npm i === npm i -S === --save === npm i 다 똑같은말
npm i -D === --saveDev 는 devDependency 에만 설치
왜냐면 배포하면 필요없는 라이브러리니까
eslint나 타입스크립트나 그런것들
타입스크립트는 배포하면 js로 변환된다 js 로 변환되서 브릿지가 ios android 언어로 바꿔줌

버전에 대해서
예를들어 2.7.0 버전이 있는데 ^2.7.0 이렇게 되어있다면
모듈설치할때 메이저는 변하지않고 나머지만 변함 그니까 ^는 막아주는거임

^2.^7.0 이렇게도 쓸수있다.

그리고 npm 트랜드에서
npm install --save react-native-device-info

react-native-device-info 라고 검색해보면
얼마나 설치했는지 볼수있고 밑에 관련 api 가 나옴

그리고 api 모듈 알아볼때 순서가
expo 기반으로 개발하면
expo -> react-native 공홈 -> 깃 외부 모듈 이렇게 알아보면된다.

외부 모듈이 맨 마지막 순위인 이유가 혹시모를 상황이 있을수도 있어서 그렇다. 지원이 끊킨다거나 해킹코드 넣는다거나
그럴수도있다.

아무거나 막 가져다 설치하면 안된다.
